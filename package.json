{
  "name": "gpu-probe",
  "version": "0.2.0",
  "description": "Drop-in WebGL2 + WebGPU scene & shader analyzer. Works as a bookmarklet or as a static script.",
  "repository": {
    "type": "git",
    "url": "https://github.com/shopify-playground/gpu-probe.git"
  },
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "serve": "python3 -m http.server 8080",
    "publish-cdn": "node scripts/publish-cdn.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
